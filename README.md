### Current list of files:
- Physical cloth simulation example with `Joint`s
- Mapping of keyboard keys to their names (both `KB_KEY`->`Str` and `Str`->`KB_KEY`);
- A simple FSM.
- Observer-Subject classes

### License
Files are in Public Domain.
